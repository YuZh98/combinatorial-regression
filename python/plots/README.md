# Plotting

Plotting code currently exists as Jupyter notebooks in this folder.

Planned structure (future):
- Convert key plotting steps into scripts that read outputs from `results/runs/`
- Write figures to `results/figures/main/` and `results/figures/supp/`

For now, reviewers can open:
- `Integer_Programming.ipynb`

